# SentCore v0.1.0 configuration roadmap placeholder.
# Runtime configuration parsing is intentionally deferred until the event model
# and collectors are stable. Current defaults are defined in AgentOptions.

